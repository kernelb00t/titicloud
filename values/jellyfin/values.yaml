persistence:
  config:
    enabled: true
    storageClass: local-config
    size: 5Gi
  media:
    enabled: true
    existingClaim: jellyfin-media-pvc

ingress:
  enabled: false

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 4000m
    memory: 4Gi
